[phases.setup]
nixPkgs = ["libxcb", "libGL", "glib", "libSM", "libXext", "libXrender"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
